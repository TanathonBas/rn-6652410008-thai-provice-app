export type Recommen_tourist = {
  id: string;
  name: string;
  district: string;
  description: string;
  latitude: number;
  longtitude: number;
  image_url: string;
};

export type Recommen_restaurant = {
  id: string;
  name: string;
  district: string;
  description: string;
  latitude: number;
  longtitude: number;
  image_url: string;
  phone: string;
};

export type Recommen_cafe = {
  id: string;
  name: string;
  district: string;
  description: string;
  latitude: number;
  longtitude: number;
  image_url: string;
  phone: string;
};

export type Recommen_temple = {
  id: string;
  name: string;
  district: string;
  description: string;
  latitude: number;
  longtitude: number;
  image_url: string;
};

export type Recommen_event = {
  id: string;
  name: string;
  district: string;
  description: string;
  image_url: string;
  event_time: string;
};
